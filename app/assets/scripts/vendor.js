// Browser testing package
import "../../temp/scripts/modernizr";
// Adds support for img's srcset and other properties in older browsers
import "picturefill";
// Adds support for only downloading images that user visits.
import "lazysizes";
